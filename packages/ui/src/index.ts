/**
 * @bnto/ui — Design system.
 *
 * Presentational components built on shadcn/ui primitives.
 * All components use dot-notation namespacing and createCn for variants.
 *
 * Structure:
 *   primitives/ — Raw shadcn/ui drops (internal, not publicly exported)
 *   components/ — Bnto wrapper components (public API)
 */

export { cn } from "./lib/utils";
