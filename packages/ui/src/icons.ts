/**
 * Icon re-exports from lucide-react.
 *
 * All icons get an `Icon` suffix to prevent naming collisions with
 * components (e.g., `Link` vs `LinkIcon`, `Menu` vs `MenuIcon`).
 *
 * Import from `@bnto/ui` — never import `lucide-react` directly in app code.
 */
export {
  LayoutDashboard as LayoutDashboardIcon,
  LogOut as LogOutIcon,
  Menu as MenuIcon,
  Play as PlayIcon,
  Search as SearchIcon,
  Settings as SettingsIcon,
  User as UserIcon,
  Workflow as WorkflowIcon,
  X as XIcon,
} from "lucide-react";

export type { LucideIcon } from "lucide-react";
