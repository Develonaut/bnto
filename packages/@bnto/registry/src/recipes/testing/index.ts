/**
 * Test-only recipe exports — not user-facing.
 *
 * These recipes exist for E2E drift detection testing only.
 * They are NOT included in the public RECIPES catalog.
 */

export { allOperations } from "./allOperations";
