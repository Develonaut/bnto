{
  "name": "@bnto/backend",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "convex dev",
    "deploy": "convex deploy",
    "build": "echo \"Convex functions are pushed via 'convex dev' or 'convex deploy'\"",
    "test": "vitest run",
    "lint": "eslint ."
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.995.0",
    "@aws-sdk/s3-request-presigner": "^3.995.0",
    "@convex-dev/auth": "catalog:",
    "convex": "catalog:"
  },
  "devDependencies": {
    "convex-test": "^0.0.41",
    "eslint": "catalog:",
    "typescript-eslint": "catalog:",
    "vitest": "catalog:"
  }
}
