"use client";

import { StepperIndicator } from "@bnto/ui";

/** Visual step indicator for the recipe flow (Files → Configure → Results). */
export function RecipeStepperIndicator() {
  return <StepperIndicator />;
}
