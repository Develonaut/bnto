//! Kitchen-sink demo module — testable field construction and help bar.
//!
//! This module is part of the library so both the demo binary and tests
//! can import it. Binary-only code (terminal, event loop, layout) lives
//! in `src/bin/demo/`.

pub mod fields;
pub mod help_bar;
