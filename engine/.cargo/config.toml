# Cargo workspace configuration.
#
# Default target is unset — `cargo test` needs native arch, wasm-pack
# handles the WASM target automatically.

[alias]
fmt-check = "fmt -- --check"
lint = "clippy --workspace --all-targets -- -D warnings"
